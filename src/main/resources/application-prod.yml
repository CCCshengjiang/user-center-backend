spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/user_center #修改为生产环境的数据库
    username: root #修改为生产环境的用户名
    password: root #修改为生产环境的密码
  session:
    timeout: 86400
server:
  address: 0.0.0.0